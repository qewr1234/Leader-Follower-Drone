# 바깥 루프 안정성 여유 분석 (2026-09-18, 2026-09-19·24 갱신)

Jetson 이 만드는 속도 명령 루프(바깥 루프)의 선형 모델을 세워 위상·이득 여유, 감도 피크, 리더→팔로워
속도 전달(스트링 안정성)을 계산하고, 실제 코드(`main.py` 제어 함수 + `imm_ekf.py`)로 돌린 비선형 체인
시뮬레이션과 대조했습니다. **이 분석이 시험 8개가 놓친 결함을 찾았고, 같은 날 코드에 수정을 적용했습니다.**
문서는 수정 전 설계와 현재 설계를 나란히 둡니다. 숫자는 전부 `analysis/stability_margins.py` 가 쓰는
[`stability_margins.json`](stability_margins.json) 에서 왔고, 아래 명령으로 재현됩니다.

```bash
python3 analysis/stability_margins.py --plots      # 표 + docs/images/stability_*.png (약 30초)
python3 analysis/stability_margins.py --identify   # IMM-EKF 주파수응답을 다시 실측 (약 40초)
```

## 1. 결론

| 구성 | PM | GM | Ms | \|Γ\| 피크 (ω) | 스트링 안정 | 정상상태 오차 (0.3 m/s) |
|---|---|---|---|---|---|---|
| P+D 단독 (KFF 0, KV 0, Kp 0.30) | 84° | 24.6 dB | 1.10 | 1.000 | 예 | 1.00 m |
| **수정 전 (2026-09-18 이전)**: KFF 0.8, FF 저역통과 0.7 s, 자기 속도 + 상대 속도, 램프 데드밴드, Kp 0.22 | 83° | **4.9 dB** | **2.31** | **1.80 (1.15 rad/s)** | **아니오** | 0.27 m |
| **직전 (2026-09-18 수정)**: + FF 저역통과 2.0 s, 자기 속도 정합 0.3 s, 소프트 데드존 | 84° | 14.4 dB | 1.29 | 1.13 (0.27) | 아니오 (1.13) | 0.45 m |
| **현재 (2026-09-19/24)**: 절대속도 추정기(자기 속도 = 예측 입력), FF 저역통과 0.1 s, 시간간격 KV 0.3 (h 1.0 s), Kp 0.30 | 102° | 16.4 dB | 1.22 | **1.000** | **예** | 0.63 m |

- **P+D 루프 자체는 여유가 큽니다.** 교차 주파수 0.31 rad/s, 위상여유 102°, 이득여유 16.4 dB, 지연 여유 5.6 s.
  이산화(10 Hz) 영향은 교차 주파수가 나이퀴스트의 1.0 % 라 무시할 수 있습니다.
- **수정 전 피드포워드는 이득여유 4.9 dB, 감도 피크 2.31 로 통상 기준(GM ≥ 6 dB, Ms ≤ 2)에 미달했고, 1.15 rad/s(주기 5.5 s)
  에서 리더 속도 변동을 1.8배 증폭했습니다.** 원인은 리더 속도를 `v_F(FC) + v̂_rel(EKF)` 로 만들 때의 지연 불일치 — 그 차이만큼
  자기 속도가 피드포워드로 **양성** 되먹임됐습니다. 2026-09-18 에는 저역통과(FF 2.0 s, 정합 0.3 s)로 눌러 14.4 dB / 1.13 을
  만들었고, SITL `leader_sine` 이 0.72배(수정 전 코드 1.95배)로 확인했습니다.
- **2026-09-19 에 그 경로 자체를 없앴습니다.** IMM-EKF 의 상태 속도를 리더 **절대** 속도로 두고 자기 속도를 예측의 알려진
  입력으로 넣으면(`p' = p + (v − v_ego)·dt`), 리더 속도 추정이 자기 속도에 (수신 지연 0.1 s 만큼 외에는) 반응하지 않습니다.
  자기 위치·속도 되먹임은 지연 없이 들어오고 피드포워드의 자기 경로는 **음성** 되먹임이 됩니다. 이득여유 14.4 → 26.6 dB
  (KV 0 기준). 이 변경의 본래 목적은 IMM 모드 확률(요구도 EST-15)이었고 안정성 개선은 부수 효과입니다.
- **남은 \|Γ\| 1.14 는 등간격 정책의 고유한 피크입니다.** FF 저역통과를 0.1 s 로 줄여도 1.05 에서 멈춥니다(7.1절). 선행 기체
  정보만 쓰는 등간격 군집은 스트링 안정이 되지 않는다는 고전 결과이고, 해법도 고전적인 **시간간격 정책**입니다: 명령에
  `−KV·v_self` 를 더하면 목표 이격이 `3.0 + (KV/Kp)·v` 로 벌어지고, 이 지연 없는 음성 되먹임이 피크를 누릅니다. KV 0.3 에서
  전후 1.000 / 좌우 1.000 / 상하 0.999 입니다.
- **시간간격 h 는 플랜트 지연에 맞춰야 합니다 (2026-09-24).** 고전 규칙 h ≥ 2τ_eff(τ_eff ≈ 0.5 s)대로 KV 0.2(h 0.67 s)는 FC 지연
  가정 0.3 s 에서만 성립하고 0.5 s 에서 1.014 로 넘습니다. KV 0.3(h 1.0 s)은 τ_fc 0.3~0.8 s 범위에서 성립합니다(7.6절).
- **대가는 정상상태 이격입니다.** (v − KFF·(v−DB) + KV·v)/Kp: Kp 0.22 그대로면 0.3 m/s 에서 0.86 m. 이득여유가 남아 **Kp 0.22 →
  0.30** 으로 되사 0.63 m(직전 0.45 m 대비 +0.18 m)입니다. Kp 0.35 부터 피크가 다시 1 을 넘습니다(7.2절).
- **실제 코드로 돌린 4단 체인**(6절)이 선형 예측과 7 % 안에서 맞고, 계단 응답의 단별 최소 간격이 3.00 m(수정 전 2.56 m)로
  오버슈트가 없습니다. SITL `leader_sine` 조건의 비선형 예측은 **0.82** (직전 설계 0.70, 실측 0.72) — 현재 파라미터의
  SITL 재실행은 아직입니다.

## 2. 모델

```
 리더 속도 v_L ─┐
                │  d = x_L − x_F  (상대 거리)              팔로워 자기 속도 v_F ──(FC, 지연 Tm)──┐
                ▼                                                                             │
   ┌─────────── IMM-EKF (실측 FRF) ────────────────────────────────────────────────────────────┤
   │  예측 입력 v_F^m = D_m·v_F,  p' = p + (v̂_L − v_F^m)·dt                                    │
   │  d̂   = E_p·x_L − [E_p(1−D_m) + D_m]·x_F        v̂_L = E_v·[x_L − (1−D_m)·x_F]           │
   │  v̂_rel = v̂_L − v_F^m                                                                     │
   └───────┬──────────────────┬──────────────────────────┬─────────────────────────────────────┘
           │                  │                          │
   Kp·(d̂−D*)          Kd·v̂_rel               KFF·H_ff·v̂_L   (H_ff = 1/(τ_ff s+1))         −KV·v_F^m  (시간간격)
           └──────────────────┴──────────────────────────┴───────────────────────────────────────┘
                    ▼ u
        H_s(s) · e^{−s·Td} · H_fc(s)  ──▶ v_F ──▶ 1/s ──▶ x_F
        평활      검출+ZOH   FC 속도루프
```

전후축 기준이며 좌우·상하는 이득만 다릅니다(`KP_RIGHT/KD_RIGHT`, `KP_UP/KD_UP`). 추정기가 자기 속도를 알려진 입력으로
받으므로 추정 오차는 자기 운동과 무관하고(선형 추정기의 성질), 자기 위치·속도는 수신 지연 `D_m = e^{−s·Tm}` 만 거쳐
되먹임됩니다. `1−D_m` 항은 "FC 가 아직 알려주지 않은 자기 운동" 이 추정기에는 리더 운동으로 보이는 몫입니다.
플랜트 입력 `u` 에서 루프를 끊고 `x_L = 0` 으로 두면

```
L(s) = P(s) · [ Kp·G_p/s + Kd·G_v + KFF·H_ff·G_ff + KV·D_m ],   P = H_s·e^{−s·Td}·H_fc
       G_p = E_p(1−D_m) + D_m,   G_v = E_v(1−D_m)/s + D_m,   G_ff = E_v(1−D_m)/s
```

네 항이 전부 음성 되먹임입니다. 리더 속도에서 팔로워 속도로의 전달은

```
Γ(s) = v_F/v_L = P·(A + B·C) / (1 + L),   A = (Kp·E_p + Kd·E_v)/s,  B = KFF·H_ff,  C = E_v/s
```

이고 n 단 체인 끝의 속도 변동은 `Γⁿ` 입니다. 모든 ω 에서 `|Γ(jω)| ≤ 1` 이면 뒤로 갈수록 진동이 커지지 않습니다
(스트링 안정성). `Γ(0) = 1` 은 구조적입니다 — 피크가 1 을 넘느냐가 문제입니다.

**직전·수정 전 설계(상대속도 추정기)** 에서는 추정기가 `d = x_L − x_F` 만 보고 자기 속도를 몰랐습니다.
`v̂_L = H_m·v_F + E_v·(x_L − x_F)` (H_m = D_m/(τ_m s+1)) 이라 루프에 `−KFF·H_ff·(H_m − E_v/s)` 라는 **양성** 되먹임 항이
남았고, 그 크기가 두 경로의 지연 차이였습니다. 이 문서의 '직전'·'수정 전' 수치는 그 모델과 그 시절 추정기 튜닝
(프레임당 고정 전이행렬, `imm_ekf.LEGACY_TUNING`)으로 계산한 것입니다.

### 가정

| 기호 | 현재 | 직전 | 수정 전 | 근거 |
|---|---|---|---|---|
| Kp, Kd (전후) | 0.30, 0.05 | 0.22, 0.05 | 같음 | `main.py` |
| KFF | 0.8 | 같음 | 같음 | `config.py` `controller.leader_vel_ff_gain` |
| KV (자기 속도 감쇠) | 0.3 (h = KV/Kp = 1.0 s) | 없음 | 없음 | `controller.self_vel_damping` |
| τ_ff | 0.1 s | 2.0 s | 0.7 s | `leader_vel_ff_tau_sec` |
| τ_m | (경로 없음) | 0.3 s | 없음 | 직전 설계의 `leader_vel_ff_self_tau_sec` |
| 추정기 | 절대속도 상태, 체류시간 전이 | 상대속도 상태, 프레임당 전이 | 같음 | `imm_ekf.py`, `LEGACY_TUNING` |
| 데드밴드 | 소프트 0.05 (기울기 1) | 같음 | 램프 0.10~0.20 (기울기 ≤ 3) | `leader_vel_ff_deadband_mps` |
| τ_s | 0.10 s | 같음 | 같음 | `smooth_velocity_cmd` alpha 0.28 @30 fps 의 등가 시정수 −Δt/ln(1−α) |
| Td | 0.10 s | 같음 | 같음 | YOLO 추론 ≈ 40 ms + 10 Hz ZOH 평균 50 ms |
| Tm | 0.10 s | 같음 | 같음 | LOCAL_POSITION_NED 10 Hz 수신 |
| τ_fc | 0.30 s | 같음 | 같음 | ArduCopter 속도루프 등가 1차 지연 가정. 0.2~1.2 s 로 흔들어 5절에서 확인 |
| E_p, E_v | 실측 | 실측 (예전 튜닝) | 같음 | 3절 |

## 3. IMM-EKF 주파수응답 실측

EKF 는 IMM 혼합·적응 R 때문에 선형화 대신 실측했습니다. `imm_ekf.ImmEkf` 에 카메라 전방축으로
`d(t) = 3 + a·sin ωt` (최대 가속 2 m/s² 이하) 를 30 Hz 로 넣고, 정착 후 위치 추정 `d̂` 와 속도 추정 `v̂` 를
최소제곱으로 맞춰 복소 이득을 뽑았습니다(0.03~30 rad/s, 28점).

![EKF FRF](images/stability_ekf_frf.png)

- 속도 추정은 1차 지연에 가깝고, 등속 출발 계단에서 **63 % 도달 0.30 s** 입니다. 예전 `leader_velocity_ff` 주석의
  가정(δ ≈ 1 s)보다 세 배 빨랐고, 그래서 정합 저역통과 τ_m 을 0.3 s 로 잡았습니다.
- 위치 추정은 램프에 정상상태 지연이 없습니다(CV 모델의 2차 추정기 특성. 8 s 후 지연 0.000 m).
- 자기 속도 입력이 0 인 실측이라 이 FRF 는 "리더 운동에 대한 추정기 응답" 입니다. 현재 추정기(체류시간 전이)와 예전
  튜닝(프레임당 전이)을 각각 실측해 JSON 에 `ekf_frf` / `ekf_frf_legacy` 로 둡니다 — 직진 운동에서는 두 응답이 사실상
  같고(63 % 응답 0.30 s 동일), 차이는 선회 판별(EST-15)에서만 납니다.

## 4. 축별 여유

### 현재 코드 (2026-09-19/24)

| 축 | Kp / Kd | ω_gc [rad/s] | PM | GM (ω_pc) | 지연 여유 | Ms | \|Γ\| 피크 (ω) |
|---|---|---|---|---|---|---|---|
| 전후 | 0.30 / 0.05 | 0.315 | 101.9° | 16.4 dB (6.24 rad/s) | 5.6 s | 1.22 | 1.000 (0.01) |
| 좌우 | 0.28 / 0.04 | 0.293 | 101.8° | 16.7 dB (6.25 rad/s) | 6.1 s | 1.21 | 1.000 (0.01) |
| 상하 | 0.18 / 0.03 | 0.189 | 104.0° | 17.3 dB (6.40 rad/s) | 9.6 s | 1.19 | 0.999 (0.01) |
| 기수 | KP_YAW 0.8 | 0.79 | 67.4° | 14.4 dB (3.3 rad/s) | 1.5 s | — | — |

### 직전 (2026-09-18 수정, 상대속도 추정기)

| 축 | ω_gc [rad/s] | PM | GM (ω_pc) | 지연 여유 | Ms | \|Γ\| 피크 (ω) |
|---|---|---|---|---|---|---|
| 전후 | 0.235 | 84.4° | 14.4 dB (1.47 rad/s) | 6.3 s | 1.29 | 1.13 (0.27) |
| 좌우 | 0.303 | 80.9° | 13.3 dB (1.51 rad/s) | 4.7 s | 1.35 | 1.17 (0.34) |
| 상하 | 0.190 | 85.1° | 14.9 dB (1.35 rad/s) | 7.8 s | 1.26 | 1.10 (0.23) |

### 수정 전 (2026-09-18 이전)

| 축 | ω_gc [rad/s] | PM | GM (ω_pc) | 지연 여유 | Ms | \|Γ\| 피크 (ω) |
|---|---|---|---|---|---|---|
| 전후 | 0.256 | 83.3° | **4.9 dB** (1.25 rad/s) | 5.7 s | **2.31** | **1.80 (1.15)** |
| 좌우 | 0.343 | 78.5° | **4.4 dB** (1.28 rad/s) | 4.0 s | **2.55** | **2.08 (1.18)** |
| 상하 | 0.202 | 84.6° | **5.3 dB** (1.20 rad/s) | 7.3 s | **2.18** | **1.63 (1.11)** |

![전후축 보드 선도](images/stability_bode_forward.png)

- 위상여유는 세 설계 다 전 축 78° 이상으로 충분합니다. 수정 전의 문제는 **이득여유** 였습니다 — 위상 교차가 1.25 rad/s
  근처에 생기는데, 그곳의 `|L|` 을 만드는 것은 P+D 항이 아니라(이미 −20 dB 아래) 피드포워드의 자기 속도 양성 되먹임
  항이었습니다. 좌우축이 이득이 커서 가장 나빴습니다(GM 4.4 dB, Ms 2.55).
- 현재 설계는 위상 교차가 6.2 rad/s 로 멀리 밀려나 있습니다. 자기 속도 경로가 음성 되먹임이 된 데다 KV 항이 지연 없는
  감쇠를 더해 주기 때문입니다. 위상여유 102° 는 과감쇠라는 뜻이고, 그 여유를 Kp 0.30 으로 일부 썼습니다.
- 기수 루프는 `cmd = KP_YAW·atan2(right, front)`, `right ≈ −front·ψ` 로 `front` 가 소거되어
  `L_ψ = KP_YAW·H_s·e^{−s(Td+Tatt)}·H_fcψ/s` 입니다(FC yaw-rate 루프 0.2 s 가정). 여유가 넉넉하고, FC yaw 루프 0.5 s
  + 지연 0.3 s 로 나빠져도 PM 48°, GM 9.2 dB 입니다. 좌우 속도와 기수가 같은 `right` 오차를 함께 줄이는 MIMO
  결합은 이 문서에서 다루지 않았습니다(8절).
- 불확실성 감속(0.75 / 0.55 배)은 루프 이득을 낮춰 GM 을 18.9 / 21.6 dB 로 올리고 \|Γ\| 는 0.999 / 0.998 입니다.
  정상 추적 상태가 가장 여유가 적습니다.

## 5. 강건성: FC 시정수 × 지연 (현재 코드)

`τ_fc` 와 `Td` 를 모르는 범위로 흔들었을 때의 전후축 PM / GM / \|Γ\| 피크입니다.

| τ_fc \ Td | 0.1 s | 0.2 s | 0.3 s | 0.5 s |
|---|---|---|---|---|
| 0.2 s | 104° / 15 dB / 1.00 | 102° / 12 dB / 1.00 | 100° / 11 dB / 1.00 | 96° / 9 dB / 1.00 |
| 0.3 s | 102° / 16 dB / 1.00 | 100° / 14 dB / 1.00 | 98° / 12 dB / 1.00 | 95° / 9 dB / 1.00 |
| 0.5 s | 98° / 19 dB / 1.00 | 96° / 16 dB / 1.00 | 95° / 14 dB / 1.00 | 91° / 11 dB / 1.01 |
| 0.8 s | 93° / 22 dB / 1.00 | 91° / 18 dB / 1.01 | 90° / 16 dB / 1.02 | 86° / 13 dB / 1.05 |
| 1.2 s | 87° / 24 dB / 1.03 | 85° / 21 dB / 1.04 | 83° / 18 dB / 1.06 | 80° / 15 dB / 1.10 |

![강건성](images/stability_robustness.png)

어느 칸에서도 GM 9 dB 아래로 내려가지 않습니다(수정 전 설계는 같은 격자에서 GM 4~8 dB). 스트링 안정은 τ_fc 0.3~0.8 s ×
Td 0.1~0.3 s 의 실용 범위에서 1.00~1.02 이고, 가장 나쁜 구석(τ_fc 1.2 s + Td 0.5 s)에서 1.10 입니다 — KV 0.2 였을 때 같은
구석은 1.19 였습니다. 시간간격 h = KV/Kp 는 플랜트 지연에 맞춰 잡는 값이라(h ≥ 2τ_eff), FC 속도루프를 SITL 속도 계단으로 식별해
τ_fc 가 0.8 s 를 넘으면 KV 를 더 올려야 합니다. 안정성 결론은 FC 모델 가정에 좌우되지 않고, 스트링 안정 결론은 약하게 좌우됩니다.

## 6. 스트링 안정성과 체인 시뮬레이션

![스트링 안정성](images/stability_string.png)

선형 예측을 실제 코드로 확인했습니다. `chain_sim` 은 리더 + 팔로워 4대를 잇고, 각 팔로워는 앞 기체와의 거리만
카메라 측정으로 받아 **실제 `ImmEkf`(자기 속도 입력 포함), 실제 `leader_velocity_ff`(수정 전은 램프 버전 재현), 실제
`compute_velocity_cmd_from_estimate`, 실제 `smooth_velocity_cmd`** 를 30 Hz 로 돌립니다(10 Hz ZOH 송신, 지연 버퍼,
FC 1차 지연, 잡음 없음). 직전·수정 전 설계는 예전 추정기 튜닝과 예전 전후 이득(0.22)으로 돌립니다.

### 선형 모델 검증 (리더 0.30 ± 0.02 m/s, 데드밴드·포화 밖)

| 구성 | ω [rad/s] | 선형 \|Γ\| | 비선형 1단 진폭비 |
|---|---|---|---|
| 현재 | 1.15 | 0.770 | 0.821 |
| 현재 | 0.35 | 0.926 | 0.950 |
| 직전 | 1.15 | 0.669 | 0.702 |
| 직전 | 0.35 | 1.115 | 1.158 |
| 수정 전 | 1.15 | 1.802 | 1.863 |
| 수정 전 | 0.35 | 1.083 | 1.127 |
| KFF=0, KV=0 | 1.15 | 0.344 | 0.379 |
| KFF=0, KV=0 | 0.35 | 0.724 | 0.753 |

전 구간 7 % 안에서 일치합니다(비선형이 조금 큽니다 — 데드존과 명령 평활의 몫). 선형 모델이 코드를 대표한다고 봐도 됩니다.

### 4단 체인 응답

![체인 계단](images/stability_chain_step.png)

| 구성 | 계단(0.3 m/s) 단별 최대 \|오차\| [m] | 단별 최소 간격 [m] | 정현파 1.15 rad/s (리더 0.15 ± 0.15) 단별 진폭비 |
|---|---|---|---|
| 현재 | 0.63, 0.63, 0.63, 0.63 | 3.00, 3.00, 3.00, 3.00 | 0.79, 0.82, 0.82, 0.82 |
| 수정 전 | 0.53, 0.61, 0.62, 0.62 | 2.80, 2.75, 2.67, 2.56 | 1.37, 1.13, 1.06, 1.03 |
| KFF=0, KV=0 | 1.00, 1.00, 1.00, 0.99 | 3.00, 3.00, 3.00, 3.00 | 0.38, 0.38, 0.38, 0.37 |

- 현재 설계는 계단에서 **단별 오차가 커지지 않고(0.63 m 그대로) 최소 간격이 3.00 m** 입니다 — 뒤 기체가 앞 기체보다
  가까이 파고드는 오버슈트가 없습니다. 수정 전은 2.80 → 2.56 m 로 단마다 줄었습니다.
- 큰 진폭 정현파에서 수정 전의 증폭이 1.37 → 1.03 으로 줄어드는 것은 `MAX_VX` 0.35 포화와 데드밴드가 진폭을
  깎기 때문입니다. 포화가 진동을 막아 주는 셈이라 `MAX_VX` 를 올렸다면 선형값 1.8 쪽으로 커졌을 것입니다.
- 데드밴드 구간(리더 0.15 ± 0.03 m/s)에서 수정 전 1단 증폭은 **3.51**, 현재는 0.82 입니다.
- 현재 설계의 자체 피크(ω → 0, |Γ| 1.000)에서 4단 진폭비는 1.000, 1.000, 1.000, 1.001 입니다.
- SITL `leader_sine` 과 같은 조건(리더 0.25 ± 0.05, 1.15 rad/s, 1단)의 비선형 예측: **현재 0.82, 직전 0.70, 수정 전
  2.31**. ArduCopter SITL 실측은 직전 설계 **0.43 / 0.72**, 수정 전 코드 **1.95** 입니다. **현재 파라미터로 SITL 을
  재실행해 0.82 근처인지 확인해야 합니다.**

## 7. 설계 선택

### 7.1 현재 추정기: FF 저역통과 τ_ff × 시간간격 KV (KFF 0.8, Kp 0.30)

칸 = \|Γ\| 피크 (ω) / GM / 정상상태 이격 [m per m/s]. 공칭 τ_fc 0.3 s 기준 — KV 열의 τ_fc 민감도는 7.6절.

| τ_ff \ KV | 0.0 | 0.1 | 0.2 | 0.3 |
|---|---|---|---|---|
| 0.1 s | 1.138 (0.46) / 20.6 dB / 0.67 | 1.051 (0.36) / 20.3 dB / 1.00 | 1.000 (0.03) / 18.4 dB / 1.33 | **1.000 (0.01) / 16.4 dB / 1.67 (현재)** |
| 0.2 s | 1.151 (0.46) / 20.5 dB / 0.67 | 1.063 (0.41) / 20.9 dB / 1.00 | 1.003 (0.20) / 18.9 dB / 1.33 | 1.000 (0.01) / 16.8 dB / 1.67 |
| 0.3 s | 1.161 (0.46) / 20.8 dB / 0.67 | 1.073 (0.41) / 21.5 dB / 1.00 | 1.009 (0.25) / 19.2 dB / 1.33 | 1.000 (0.01) / 17.0 dB / 1.67 |
| 0.5 s | 1.176 (0.44) / 21.5 dB / 0.67 | 1.089 (0.36) / 22.1 dB / 1.00 | 1.024 (0.27) / 19.4 dB / 1.33 | 1.000 (0.01) / 17.1 dB / 1.67 |
| 1.0 s | 1.192 (0.37) / 22.9 dB / 0.67 | 1.114 (0.33) / 22.5 dB / 1.00 | 1.053 (0.26) / 19.6 dB / 1.33 | 1.010 (0.16) / 17.2 dB / 1.67 |
| 2.0 s | 1.192 (0.29) / 23.8 dB / 0.67 | 1.131 (0.26) / 22.6 dB / 1.00 | 1.081 (0.22) / 19.6 dB / 1.33 | 1.042 (0.19) / 17.2 dB / 1.67 |

- **KV = 0 열**: 절대속도 추정기만으로는 피크가 1.14~1.19 이고 τ_ff 를 줄여도 1.05 아래로 안 내려갑니다. 이것이 등간격
  정책의 고유한 피크입니다 — 피드포워드가 아무리 정확해도 P 항이 이격 오차에 반응하며 겹칩니다.
- **τ_ff 행**: 직전 설계에서 2.0 s 로 길게 잡은 이유(양성 되먹임 경로를 공진 아래로)가 사라지자, 긴 저역통과는 FF 를
  P 응답보다 늦게 만들어 피크를 키우는 쪽으로만 작용합니다. 0.1 s 는 호버 잡음을 누르는 최소값입니다 — 정지 리더 +
  측정 잡음(σ 0.15/0.25 m)에서 리더 속도 추정 σ 0.07 m/s 가 FF 명령 기여 σ 0.03 m/s 로 들어옵니다(2.0 s 면 0.008).
- **KV 0.2 vs 0.3**: 공칭 τ_fc 에서는 둘 다 1.000 이지만, 0.2 는 τ_fc 0.5 s 에서 1.014 로 넘고 0.3 은 0.8 s 까지 버팁니다(7.6절).

### 7.2 Kp 로 이격 되사기 (KV 0.3, τ_ff 0.1)

| Kp | \|Γ\| 피크 | GM | PM | Ms | 정상상태 이격 증가 [m per m/s] | 0.3 m/s 에서 [m] |
|---|---|---|---|---|---|---|
| 0.22 | 0.999 | 16.7 dB | 104° | 1.21 | 2.27 | 0.86 |
| 0.26 | 0.999 | 16.6 dB | 103° | 1.21 | 1.92 | 0.73 |
| 0.30 (현재) | 1.000 | 16.4 dB | 102° | 1.22 | 1.67 | 0.63 |
| 0.35 | 1.000 | 16.2 dB | 101° | 1.23 | 1.43 | 0.54 |

시간간격 항이 늘린 이격(0.3 m/s 에서 +0.30 m)을 Kp 로 되삽니다. 0.30 이 피크 1.000 을 지키는 상한이고, 0.35 부터 넘습니다.
좌우·상하 이득은 그대로 두었습니다(이격 개념이 없어 되살 것이 없고, 피크가 이미 ≤ 1.000).

### 7.3 KFF × Kd (τ_ff 0.1, KV 0.3, Kp 0.30)

칸 = PM / GM / \|Γ\| 피크 / 정상상태 이격 [m per m/s, 데드존 제외].

| KFF \ Kd | 0.05 (현재) | 0.10 | 0.15 |
|---|---|---|---|
| 0.6 | 102° / 16.6 dB / 0.999 / 1.33 | 105° / 15.6 dB / 0.999 / 1.33 | 108° / 14.7 dB / 0.999 / 1.33 |
| 0.7 | 102° / 16.5 dB / 1.000 / 1.00 | 105° / 15.5 dB / 0.999 / 1.00 | 108° / 14.6 dB / 0.999 / 1.00 |
| 0.8 (현재) | 102° / 16.4 dB / 1.000 / 0.67 | 105° / 15.4 dB / 1.000 / 0.67 | 108° / 14.6 dB / 1.000 / 0.67 |
| 0.9 | 102° / 16.3 dB / 1.000 / 0.33 | 105° / 15.3 dB / 1.000 / 0.33 | 108° / 14.5 dB / 1.000 / 0.33 |
| 1.0 | 102° / 16.2 dB / 1.035 / 0.00 | 105° / 15.2 dB / 1.031 / 0.00 | 108° / 14.4 dB / 1.028 / 0.00 |

KFF 0.9 이상은 피크가 다시 넘고, Kd 는 위상여유만 올릴 뿐 피크를 바꾸지 못합니다. KFF 0.8 / Kd 0.05 를 유지합니다.

### 7.4 [기록] 직전 설계의 선택: τ_ff × τ_m (상대속도 추정기, KFF 0.8, Kp 0.22)

칸 = GM / Ms / \|Γ\| 피크(ω) / 양성 되먹임 경로 피크. 2026-09-18 의 결정 근거이며, 현재 추정기에는 τ_m 이라는 항 자체가 없습니다.

| τ_ff \ τ_m | 0 | 0.3 s | 0.5 s |
|---|---|---|---|
| 0.7 s | **4.9 dB / 2.31 / 1.80 (1.15) / 0.46 (수정 전)** | 8.7 dB / 1.60 / 1.29 (1.00) / 0.27 | 11.1 dB / 1.42 / 1.12 (0.94) / 0.17 |
| 1.0 s | 6.5 dB / 1.91 / 1.44 (0.99) / 0.37 | 10.3 dB / 1.47 / 1.17 (0.76) / 0.22 | 12.6 dB / 1.35 / 1.11 (0.35) / 0.13 |
| 1.5 s | 8.8 dB / 1.60 / 1.18 (0.72) / 0.28 | 12.7 dB / 1.35 / 1.12 (0.35) / 0.16 | 14.6 dB / 1.28 / 1.12 (0.28) / 0.10 |
| 2.0 s | 10.6 dB / 1.46 / 1.13 (0.35) / 0.22 | **14.4 dB / 1.29 / 1.13 (0.27) / 0.13 (직전)** | 16.0 dB / 1.25 / 1.12 (0.24) / 0.08 |
| 3.0 s | 13.3 dB / 1.32 / 1.13 (0.22) / 0.16 | 16.6 dB / 1.23 / 1.13 (0.21) / 0.09 | 17.4 dB / 1.22 / 1.13 (0.20) / 0.05 |

### 7.5 데드밴드

수정 전: `target = v·clamp((|v|−DB)/DB, 0, 1)`, DB 0.10 → DB < |v| < 2DB 에서 국소 기울기 최대 3.
현재: `target = v·max(0, 1 − DB/|v|)`, DB 0.05 → 기울기 1. 빼는 만큼 정상상태 오차가 `KFF·DB/Kp` 늘어(0.13 m) 폭을 반으로
줄였습니다. 호버 잡음은 τ_ff 가 짧아진 지금은 추정기(E_v)와 명령 평활이 평균내며, 데드존은 바이어스만 막으면 됩니다.

### 7.6 외부 레퍼런스와의 대조 (2026-09-24)

같은 종류의 시스템이 어떻게 설계돼 있는지, 그리고 고전 결과가 이 설계에 무엇을 요구하는지 대조했습니다.

**대역 분리.** 캐스케이드 제어의 경험칙은 내부 루프가 외부보다 3~5배 빨라야 한다는 것입니다. ArduCopter 의 속도루프
이득 `PSC_NE_VEL_P` 기본값은 2.0 (m/s² per m/s) 이라 내부 속도루프 극점이 대략 2 rad/s(시정수 0.5 s) 이고, 이 문서의
가정 τ_fc 0.3 s 는 3.3 rad/s 입니다. 외부 루프 교차 주파수 0.30 rad/s 와의 비율은 **6.7~11배** — 규칙 안에 여유 있게
듭니다. 지연 여유 5.5 s 는 비전 파이프라인 지연(0.1~0.3 s)의 20배 위입니다.

**스트링 안정성과 시간간격.** Swaroop–Hedrick(1996) 이후의 고전 결과: 등간격 정책은 선행 기체 정보만으로는 스트링 안정이
불가능하고, 시간간격 정책에서 1차 지연 τ 를 가진 PD 추종은 **h ≥ 2τ** 일 때 스트링 안정입니다(Rajamani). 선행 기체의
속도·가속도 피드포워드(CACC)가 있으면 이 요구가 완화됩니다(Ploeg, Naus). 이 설계는 정확히 그 구조 — 시간간격 h = KV/Kp,
피드포워드 KFF 0.8 — 이고, 6절의 선형 모델이 그 완화를 정량화한 것입니다. 문제는 완화가 **τ 가정에 기댄다**는 점입니다.

| KV | h [s] | τ_fc 0.3 / Td 0.1 | 0.5 / 0.1 | 0.8 / 0.1 | 0.3 / 0.3 | 0.5 / 0.3 | 0.8 / 0.3 | e_ss @ 0.3 m/s |
|---|---|---|---|---|---|---|---|---|
| 0.20 | 0.67 | 1.000 | **1.014** | **1.049** | **1.017** | **1.046** | **1.087** | 0.53 m |
| 0.25 | 0.83 | 1.000 | 1.000 | **1.019** | 1.000 | **1.013** | **1.049** | 0.58 m |
| **0.30 (현재)** | **1.00** | 1.000 | 1.000 | 1.001 | 1.000 | 1.000 | **1.018** | 0.63 m |
| 0.40 | 1.33 | 0.999 | 1.000 | 1.000 | 1.000 | 1.000 | 1.000 | 0.73 m |

칸 = \|Γ\| 피크. 등가 지연 τ_eff ≈ τ_fc + Td + τ_s 가 공칭 0.5 s 이므로 고전 규칙은 h ≥ 1.0 s 를 요구하고, 표가 그것을
그대로 재현합니다: KV 0.2(h 0.67 s)는 τ_fc 0.3 s 에서만 성립하고 ArduCopter 기본 이득이 시사하는 0.5 s 에서 이미 넘습니다.
**그래서 KV 를 0.2 → 0.3 (h = 1.0 s) 로 올렸습니다.** 대가는 0.3 m/s 이격 +0.10 m 와 GM 18.4 → 16.4 dB 이고, 두 값 다
여유 안입니다. 남는 구석은 τ_fc 0.8 s **와** 전송 지연 0.3 s 가 겹칠 때의 1.02 — 둘 다 공칭의 2.7배·3배라 실측으로 배제할
수 있는 값이고, 그렇지 않으면 KV 0.4 가 그 구석도 덮습니다(이격 +0.10 m 추가). 단독 추종에서는 \|Γ\| 1.05 도 해가 없지만, 체인 운용을 열어 두려면 가정이 아니라 범위로 보증하는 쪽이 맞습니다.

**레퍼런스 follow 구현과의 비교.**

| 항목 | 이 설계 | ArduPilot `AP_Follow` | PX4 follow-me (`FlightTaskAutoFollowTarget`) | Vision leader-follower (arXiv 2609.01420) |
|---|---|---|---|---|
| 표적 정보 | RGB-D 30 Hz, cm 급 | GPS 1~5 Hz, m 급 | GPS 1~5 Hz | RGB-D + YOLOv8, 20~25 Hz |
| 추정기 | IMM-EKF (CV+CT), 절대속도 상태 | 등속 외삽 | 알파-베타-감마 + 2차 필터 (ω_n 1.0 rad/s, ζ 0.707) | CV 칼만 필터 |
| 제어 | P+D + 속도 FF 0.8 + 시간간격 | P (`FOLL_POS_P` 0.1) + 속도 FF, 가속 5 m/s²·저크 10 m/s³ 성형 | 위치 setpoint + 필터된 속도 FF | 분리 PID |
| 표적 소실 | 2 s 코스트 + 8 s 홀드 → LAND | `FOLL_TIMEOUT` 3 s | 추정기 1.5 s 타임아웃 → 필터 리셋 | (미기술) |
| 근접 보호 | 2 m 장벽 + 1.5 m 측면 회피 | `FOLL_DIST_MAX` 100 m(유효성만) | yaw 제어 최소 거리 1.0 m, 안전 고도 1.0 m | (미기술) |
| 보고 성능 | 선형 여유 + SITL(직전 설계 0.72) | — | — | 위치 RMSE 0.58 m (시뮬) |

이 설계의 위치 P 이득 0.30 은 `AP_Follow` 의 0.1 보다 세지만, 저쪽은 m 급 GPS 를 1~5 Hz 로 받는 구조라 낮을 수밖에 없고,
이쪽은 4절의 여유가 그 값을 뒷받침합니다. PX4 가 표적 속도를 1 rad/s 2차 필터로 크게 늦추는 것(그리고 "표적이 멈추면
오버슈트한다" 는 주석)은 이 문서 7.1절이 τ_ff 를 2.0 → 0.1 s 로 줄인 결정과 반대 방향인데, 저쪽은 GPS 잡음 억제가 목적이고
이쪽은 추정기(E_v)가 이미 그 역할을 하기 때문입니다. PX4 의 "yaw 제어 최소 거리 1.0 m" 는 이 설계에 없는 것으로, 외부
감사에서 낮은 심각도로 남아 있습니다.

## 8. 한계

- SISO 축별 분석입니다. 좌우 속도와 기수가 함께 `right` 오차를 줄이는 결합, 자세 보상이 축을 섞는 효과는 포함하지
  않았습니다.
- FC 속도루프를 1차 지연으로 놓았습니다. ArduCopter 의 실제 응답(`PSC_VELXY_P`, 가속 제한, 자세 루프)은 SITL 에서
  속도 계단으로 식별해 `τ_fc` 를 확정해야 합니다. 5절 표는 그 전까지의 범위 보증입니다.
- 측정 잡음, 검출 누락, 포화(`MAX_V*`)는 선형 모델에 없습니다. 체인 시뮬레이션에는 포화·데드밴드가 있고 잡음은 없습니다.
- 기수 루프의 EKF 경로는 ego-yaw 보정이 즉시 적용된다고 보고 ATTITUDE 수신 지연 0.1 s 만 넣었습니다.
- 실제 FC 검증은 SITL(`leader_sine`) 까지이고, 그것도 **직전 설계**의 실측입니다. 현재 파라미터(Kp 0.30, KV 0.2, τ_ff 0.1,
  절대속도 추정기)는 아직 SITL 로 재확인하지 않았습니다(예측 0.82). 실기의 FC 속도루프·바람·측정 잡음은 포함되지 않았습니다.
- 시간간격 정책의 스트링 안정은 τ_fc 가정에 민감합니다(5절). FC 속도루프가 0.3 s 보다 느리면 KV 를 올려야 합니다.

## 9. 파일

| 파일 | 내용 |
|---|---|
| `analysis/stability_margins.py` | 모델·여유 계산·체인 시뮬레이션·그림. `BEFORE` 가 수정 전 설계 |
| `docs/stability_margins.json` | 계산 결과와 EKF 실측 FRF (문서 숫자의 원본, `analysis/trace_check.py` 가 참조) |
| `docs/images/stability_*.png` | 보드 선도, 스트링 안정성, 체인 응답, 강건성, EKF FRF |
| `test_fixes.py` `분석:` 검사 | 기준선(P+D) 여유, 수정 전 골든값(결함의 기록), 현재 설계 여유를 회귀로 고정 |
| `sitl/harness.py` `leader_sine` | 같은 조건을 실제 ArduCopter 에서 검사하는 시나리오 |
